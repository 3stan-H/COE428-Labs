Tristan Hsiung 501311581

Suppose that towers(5, 2, 3) is invoked.
1. How will the first recursive call to towers() be invoked? Answer this
question in the form: towers(x, y, z) where you give the actual values to the
three parameters.

towers(4, 2, 1) 

2. How many recursive calls to towers() will be made before this first recursive
call actually returns to the initial invocation?

62 calls will be made since it follows the pattern 2^(n+1) - 2, where n is the number of rings needed to be sorted.
(towers with a ring count of 4 is invoked twice, ring count 3 is invoked 4 times, 2 is 8 times, 1 is 16 times and 0 is 32 times.)


3. Once towers(5, 2, 3) has invoked its first recursive call to towers() and this
invocation has returned, what will be printed to stdout? (i.e. What actual
move will be made by towers(5, 2, 3) ?

2 3

4. How will the second recursive call to towers() be invoked? Answer this
question in the form: towers(x, y, z) where you give the actual values to the
three parameters.

towers(3, 2, 3)

Suppose that towers(8, 1, 2) is invoked. How many lines will be printed 

255 lines will be printed. (Contained in junk3)

A brief description of what you did (and did not) achieve in the Lab

After the lab, I was able to explore recursion to help with the Towers of Hanoi. Through tracing
I was able to understand how the algorithms used worked. I also learned how to seperate program 
output using stdout and stderr. I also learned how to modify the main function to correctly 
handle command line arguments. 
